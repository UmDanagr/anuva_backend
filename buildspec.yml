version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies..."
      - npm ci
  build:
    commands:
      - echo "Building Vite project..."
      - npm run build
artifacts:
  files:
    - '**/*'
  base-directory: dist
  discard-paths: no
